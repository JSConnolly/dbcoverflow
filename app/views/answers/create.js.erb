<% if @answer.errors.empty? %>
	$('.answers').append("<%= j render 'answer', :answer => @answer %>");
	$('#answer_content').val('');
<% else %>
	$('.answers').append('<%= j @answer.errors.full_messages.join("\n")%>');
<% end %>
